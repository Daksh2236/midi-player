41. I see the notes i added with the duration and also the instrument
    Yes, you could play the tune on a piano
    
43. Yes, you can play the tune more than once because the notes are always in the simpletune object

45. external method:
    tune.getNotes()
    
    internal method:
    sequence.createTrack();
    
58. I see that is switches classes it went from TuneCreator to SimpleTune to Note
    For the local variable we see the note it is supposed to play
    You also see all the methods that are called 
    It shows how a note is added to the tune                     